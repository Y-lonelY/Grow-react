import MainView from '../view/MainView'

const config = {
    routeConfig: [{
        key: 1,
        title: 'mainView',
        exact: true,
        path: '/',
        component: MainView
    }]
}

export default config;